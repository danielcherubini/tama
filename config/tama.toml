[general]
log_level = "info"

[backends.llama_cpp]
# version = "b8407"         # pin a specific version; omit to use the latest installed
# path = "/custom/path/llama-server"  # or point directly to a binary (custom installs)
# default_args = []         # extra CLI args passed to every invocation
health_check_url = "http://localhost:8080/health"

[backends.ik_llama]
# version = "main@abc1234"  # for source builds, use the git hash logged at install
# path = "/custom/path/ik-llama-server"
health_check_url = "http://localhost:8080/health"

[models]

[supervisor]
health_check_interval_ms = 10000
health_check_timeout_ms = 30000
health_check_retries = 3

[proxy]
# How long to keep system-metric samples in the SQLite history table.
# Older rows are pruned on every metrics tick. Default: 86400 (24 hours).
metrics_retention_secs = 86400
